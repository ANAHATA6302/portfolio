[build]
# This command sources the correct path for asdf,
# which then reads your .tool-versions file to set up Java 17.
command = "source /opt/build/build-image/etc/asdf.sh && ./gradlew jsBrowserDistribution --no-daemon"

# Directory to publish
publish = "composeApp/build/dist/js/productionExecutable/"